# =============================================================================
# daflash.com Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Sanity CMS Configuration
# -----------------------------------------------------------------------------
# Get these from https://www.sanity.io/manage
# Project ID: Found in your Sanity project settings
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id

# Dataset: Usually "production" for live site
NEXT_PUBLIC_SANITY_DATASET=production

# API Token: Required for admin mutations (create/update/delete)
# Generate at: Sanity Dashboard > API > Tokens > Add API Token
# Permissions: Editor or higher
SANITY_API_TOKEN=your_sanity_api_token

# -----------------------------------------------------------------------------
# Google OAuth (for admin authentication)
# -----------------------------------------------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Application type: Web application
# Authorized redirect URIs: https://daflash.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# -----------------------------------------------------------------------------
# NextAuth.js Configuration
# -----------------------------------------------------------------------------
# Secret: Generate with: openssl rand -base64 32
# Used to encrypt session cookies
NEXTAUTH_SECRET=generate_a_strong_secret_here

# URL: Your site's canonical URL (no trailing slash)
# Development: http://localhost:3000
# Production: https://daflash.com
NEXTAUTH_URL=https://daflash.com

# -----------------------------------------------------------------------------
# Admin Access Control
# -----------------------------------------------------------------------------
# Comma-separated list of email addresses allowed to access /admin
# SECURITY: If this is not set, NO ONE can sign in (deny by default)
# Example: arik@daflash.co,another@example.com
ADMIN_ALLOWED_EMAILS=arik@daflash.co
